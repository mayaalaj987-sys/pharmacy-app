# Base origin of the Laravel Gate 1C backend. No trailing slash.
# Must be a host explicitly listed in the backend's ADMIN_ALLOWED_ORIGINS
# and CORS-credentialed. Never a wildcard, never a production shared secret.
VITE_ADMIN_API_BASE_URL=http://localhost:8000
